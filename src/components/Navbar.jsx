function Navbar() {
  return (
    <nav className="navbar">
      <h2 className="logo">Hero Clash</h2>

      <div className="nav-links">
        <a href="#">Home</a>
        <a href="#">Battle</a>
        <a href="#">Favorites</a>
      </div>
    </nav>
  );
}

export default Navbar;